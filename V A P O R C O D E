function vaporcode(string) {

  let spltStr = string.split(' ')
  let newArr = [];

  for( let i = 0; i < spltStr.length; i++ ) {
    let splt = spltStr[i].split('').join('  ').toUpperCase()
    newArr.push(splt + ' ')
    
  }
  return newArr.join(' ').trim()
  
}
