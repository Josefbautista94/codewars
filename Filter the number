var FilterString = function(value) {
  //Complete this function :)
  let splitValue = value.split('')
  let numArr = []
let nums = splitValue.filter((item)=>{
    if(item <0|| item >= 0)
      numArr.push(item)
  })
   return Number(numArr.join(""))
}
