function sentencify(words) {
 
  let sentence = '';
  
  for(let i = 0; i < words.length; i++){
    sentence += words[i]+ ' ';
    }
  
 let newS =sentence.charAt(0).toUpperCase() + sentence.slice(1);
  
  return newS.trim() + ".";
 }
