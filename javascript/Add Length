function addLength(str) {
//start-here
  
  
  let splitString = str.split(' ');
  
  let newArr = []
  
  for(let i = 0 ; i < splitString.length; i ++){
    
    newArr.push(splitString[i] + ' ' + splitString[i].length);
  }
  
  return newArr
}
