function shortcut (string) {

  let vowels = 'aeiou';
  let i = 0;
  let answer = [];
  
  while( i < string.length){
    
    if(!vowels.includes(string[i]))
    answer.push(string[i]);
    i++;
    
  }
  
  return answer.join('');
}
