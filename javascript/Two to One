function longest(s1, s2) {
  
  let newArr1 = s1.split('')
  let newArr2 = s2.split('')
  newArr1.push(...newArr2);
  newArr1.sort()
  let answer = [...new Set(newArr1)]
  
  return answer.join('')
}
