function spinWords(string){
  let sp = string.split(' ');
  
  for(let i = 0 ; i < sp.length; i++){
    if(sp[i].length >= 5)
    sp[i] = sp[i].split('').reverse().join('');
      }
  return sp.join(' ');
}
