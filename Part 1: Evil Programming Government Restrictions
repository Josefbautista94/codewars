var anarchy = function (d) {
  
  let loL = new Set(['This'])
  let lOl = new Set(['is' , 'way'])
  let lOL = new Set(['Too', 'easy', 'LOL']) 
  
 return 'I can write numbers like, ' + loL.size + ', ' + lOl.size + ', ' + lOL.size + '.';
}
